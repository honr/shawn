prepare_world edge_model=list comm_model=disk_graph range=10 size_hint=10

create_routing_protocol name=my_app_routing protocol=geo
routing_protocol=my_app_routing

send_interval=1

rect_world width=100 height=100 count=300 processors=geo_test 

simulation max_iterations=100

#Options for geo_test: draw_shortest_path, draw_euclidean_dist, draw_comm_range, draw_dest_area

draw_shortest_path=true
draw_euclidean_dist=false
draw_comm_range=false
draw_dest_area=true

geo_test psfile=geo_test.eps

