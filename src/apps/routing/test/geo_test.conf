prepare_world edge_model=list comm_model=disk_graph range=10 size_hint=10

create_routing_protocol name=my_app_routing protocol=geo

routing_test_protocol=my_app_routing
routing_test_eval_round=10
routing_test_psfile=routing_test.ps
routing_test_draw_topology=false

rect_world width=100 height=100 count=500 processors=routing_test 

simulation max_iterations=1000

